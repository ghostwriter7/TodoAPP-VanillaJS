{
  "name": "todoapp",
  "version": "0.0.1",
  "description": "A Demo TodoApp built with Vanilla JS",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"vite\" \"firebase emulators:start\" ",
    "build": "vite build",
    "deploy": "firebase deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ghostwriter7",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cypress": "^13.6.2",
    "firebase-tools": "^13.0.2",
    "rollup-plugin-copy": "^3.5.0"
  },
  "dependencies": {
    "firebase": "^10.7.1",
    "vite": "^5.0.10"
  }
}
